<Window x:Class="WpfMovieRental.wpfrental"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMovieRental"
        mc:Ignorable="d"
        Title="Rental" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Days Rentle:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox x:Name="txtDaysRentle" Grid.Row="0" Margin="100,0,0,5" Width="250"/>

        <TextBlock Text="Customer Id:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="txtCustomerId" Grid.Row="1" Margin="100,0,0,5" Width="250"/>

        <TextBlock Text="Movie Id:" Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox x:Name="txtMovieId" Grid.Row="2" Margin="100,0,0,5" Width="250"/>

        <TextBlock Text="Customer Name:" Grid.Row="3" VerticalAlignment="Center"/>
        <TextBox x:Name="txtCustomerName" Grid.Row="3" Margin="100,0,0,5" Width="250"/>

        <TextBlock Text="Payment Method:" Grid.Row="4" VerticalAlignment="Center"/>
        <TextBox x:Name="txtPaymentMethod" Grid.Row="4" Margin="100,0,0,5" Width="250"/>

        <TextBlock Text="Ammount:" Grid.Row="5" VerticalAlignment="Center"/>
        <TextBox x:Name="txtAmmount" Grid.Row="5" Margin="100,0,0,5" Width="250"/>

        <Button Content="Save Rentle"
                Grid.Row="3"
                Margin="0,10,0,0"
                Width="150"
                Height="30"
                HorizontalAlignment="Left"
                Click="BtnSave_Click"/>

        <TextBlock x:Name="lblStatus"
                   Grid.Row="4"
                   Margin="0,20,0,0"
                   Foreground="Green"
                   FontWeight="Bold"
                   TextAlignment="Center"/>
    </Grid>
</Window>
